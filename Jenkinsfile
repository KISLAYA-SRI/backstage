pipeline {
    agent any
    stages {
	// Echo
	stage('Publish message') {
	    steps{
              script {
		      while:
		      do
			echo "Hello World! ${key}"
		      done
              }
            }
	}
   }
}
