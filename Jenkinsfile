pipeline {
    agent any
    stages {
	// Echo
	stage('Publish message') {
	    steps{
              script {
		      while(true)
		      do
			echo "Hello World! ${key}"
		      done
              }
            }
	}
   }
}
